import React from 'react';

function PrimoResult({ resultado}) {
    return (
        <div>
            <p>El n-ésimo número Primo es: {resultado}</p>
        </div>
    )
}

export default PrimoResult;